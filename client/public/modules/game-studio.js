import module from '@sillonious/module'

const $ = module('game-studio')

$.draw(() => {
  return `
    todo:
    <a href="https://editor.godotengine.org/releases/latest/">
      embed godot
    </a>
  `
})
