import module from '@sillonious/module'

const $ = module('system-shell')

$.draw(() => {
  return `
    todo:
    <a href="https://github.com/ktock/container2wasm">
      bootstrap the entire system within itself
    </a>
  `
})
